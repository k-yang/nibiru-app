import { Layout } from "@/components";
import AddressSection from "@/components/address/AddressSection";

export default function AddressTester() {
  return (
    <Layout>
      <AddressSection />
    </Layout>
  );
}
